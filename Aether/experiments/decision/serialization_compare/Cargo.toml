[package]
name = "serialization_compare"
version = "0.0.0"
edition = "2021"
publish = false
description = "Phase 0 decision experiment: DEC-003 serialization format comparison"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ciborium = "0.2"
hex = "0.4"
sha2 = "0.10"
