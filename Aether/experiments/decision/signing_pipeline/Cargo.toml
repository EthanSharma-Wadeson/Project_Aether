[package]
name = "signing_pipeline"
version = "0.0.0"
edition = "2021"
publish = false
description = "Phase 0 decision experiment: DEC-004B full canonical CBOR signing pipeline"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ciborium = "0.2"
ed25519-dalek = "2"
sha2 = "0.10"
hex = "0.4"
